// Cloudinary Configuration Example
// Copy this file to cloudinary.js and fill in your actual values

export const cloudinaryConfig = {
    // Your Cloudinary cloud name
    cloudName: 'your-cloud-name',

    // Upload preset (must be created in Cloudinary dashboard)
    // This preset should be set to 'unsigned' for client-side uploads
    uploadPreset: '',
};

export default cloudinaryConfig;
